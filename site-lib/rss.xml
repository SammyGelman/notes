<rss xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:atom="http://www.w3.org/2005/Atom" version="2.0"><channel><title><![CDATA[html_export]]></title><description><![CDATA[Obsidian digital garden]]></description><link>http://github.com/dylang/node-rss</link><image><url>site-lib/media/favicon.png</url><title>html_export</title><link/></image><generator>Webpage HTML Export plugin for Obsidian</generator><lastBuildDate>Wed, 13 May 2026 15:11:56 GMT</lastBuildDate><atom:link href="site-lib/rss.xml" rel="self" type="application/rss+xml"/><pubDate>Wed, 13 May 2026 15:11:56 GMT</pubDate><ttl>60</ttl><dc:creator/><item><title><![CDATA[index]]></title><description><![CDATA[Task-agnostic pre-screening pipeline for OMOP concept_id extraction. Runs once per task. Produces an exhaustive list of relevant concept_ids per OMOP table and per concept-id column, ready to be handed to downstream event-extraction code.For each clinical event-detection task (defined in a small YAML file):
Asks Nemotron which OMOP tables to search for this task.
For each selected table, runs&nbsp;two passes&nbsp;— one against the standard concept-id column, one against the source concept-id column.
Per pass: asks Nemotron for a wide net of regex patterns, applies them to the OMOP&nbsp;concept&nbsp;table, expands hits to their descendants via&nbsp;concept_ancestor, matches against the cohort's data, and asks Nemotron to filter the survivors for clinical relevance.
The output is a per-task directory with the final concept_id list, plus the intermediate artifacts at every stage of the funnel so you can audit and diagnose. ┌─────────────────────────────────┐ │ Nemotron #0: table selector │ one call per task └─────────────────────────────────┘ │ ▼ chosen tables │ ─── per (task, table) ───────────────────────────────────────────────────── │ splits into two passes: ┌─────────────────┬─────────────────┐ │ standard pass │ source pass │ │ *_concept_id │ *_source_concept_id │ domain filter on │ domain filter off │ standard_only=T │ standard_only=F │ └─────────────────┴─────────────────┘ │ ▼ vocab metadata for this column (which vocabularies live here) │ ▼ ┌─────────────────────────────────┐ │ Nemotron #1: wide-net regex │ └─────────────────────────────────┘ │ ▼ include_patterns / exclude_patterns ┌─────────────────────────────────┐ │ concept-table query │ filters: domain, └─────────────────────────────────┘ vocabularies, standard │ ▼ wide_net_seeds ┌─────────────────────────────────┐ │ descendant expansion │ via concept_ancestor; └─────────────────────────────────┘ re-applies filters │ ▼ wide_net_concepts (+ match_type: regex_seed | descendant) ┌─────────────────────────────────┐ │ cohort match │ count rows per concept_id └─────────────────────────────────┘ in cohort data │ ▼ cohort_match_concepts ┌─────────────────────────────────┐ │ Nemotron #2: relevance filter │ batched, json_schema strict └─────────────────────────────────┘ │ ▼ final_concepts (per pass) │ unioned across all (table, pass) into final_candidates.parquet
Each table has two searchable columns: the canonical standard&nbsp;*_concept_id&nbsp;and the source&nbsp;*_source_concept_id&nbsp;holding non- standard codes (Epic EDG, ICD9CM source, MSDW Attrib Catalog, etc.). Both are searched per task by default.The table-selector LLM call picks which of these to actually run for a given task. Override via&nbsp;tables:&nbsp;in the task YAML for explicit control.prescreen/ __init__.py config.py # paths + EVENT_TABLES (standard + source concept_col per table) cohort.py # load_cohort, cohort_ids (cached parquet) concept_table.py # cached concept table; coerces string concept_id -&gt; Int64 metadata.py # reads outputs/vocab_prescreen/{table}_*_vocab_type.csv vocab.py # query_concept(include, exclude, domain, vocabularies, standard_only) expand.py # descendant expansion via concept_ancestor cohort_match.py # count_concepts_in_cohort(table, candidate_ids, concept_col) nemotron.py # NemotronClient (mirrors clinical_extractor.client) llm_steps.py # prompts + json_schemas (table-selector, wide-net, relevance) pipeline.py # prescreen_one_table, prescreen_task, save_results tasks/ n_acut_06_hypoglycemia.yaml run_prescreen.py # CLI runner
diagnose.py # walk artifacts and find where a table's funnel collapsed
verify_coverage.py # check whether a known list of concept_ids was captured
# 1. Confirm the vLLM is reachable
python run_prescreen.py --healthcheck # 2. Run one task
python run_prescreen.py tasks/n_acut_06_hypoglycemia.yaml # 3. Fan out across all tasks
python run_prescreen.py tasks/*.yaml
Per task, under&nbsp;sammy/prescreen/tasks_out/{task_tag}/:hypoglycemia/ task.json run_summary.json final_candidates.parquet # unified across all (table, pass) # per (table, pass) artifacts — one set per pass: condition_occurrence__standard__patterns.json condition_occurrence__standard__vocab_metadata.parquet condition_occurrence__standard__wide_net_seeds.parquet condition_occurrence__standard__wide_net_concepts.parquet condition_occurrence__standard__cohort_match_concepts.parquet condition_occurrence__standard__decisions.parquet condition_occurrence__standard__final_concepts.parquet condition_occurrence__source__patterns.json condition_occurrence__source__vocab_metadata.parquet ... drug_exposure__standard__... drug_exposure__source__... measurement__standard__... measurement__source__...
final_candidates.parquet&nbsp;carries&nbsp;source_table,&nbsp;search_pass&nbsp;(standard&nbsp;/&nbsp;source), and&nbsp;source_concept_col&nbsp;columns so each concept tells you which (table, pass) it came from.diagnose.py {task_tag} {table}&nbsp;— walks the artifacts for one table, prints sizes at each stage of both passes, and tells you the first empty stage. Use it whenever a table comes back lighter than expected.python diagnose.py hypoglycemia condition_occurrence # both passes
python diagnose.py hypoglycemia condition_occurrence --pass source
verify_coverage.py {task_tag} --csv known_ids.csv&nbsp;— given a known list of concept_ids (e.g. from a gold-standard phenotype, a prior chart-review set, or an ad-hoc cohort check), reports what fraction the prescreen captured, broken down by which pass found them, and prints the names of anything missed.python verify_coverage.py hypoglycemia --csv my_known_glycemia_ids.csv
python verify_coverage.py hypoglycemia --ids 4129519,4226798,4280953
A minimal task only needs&nbsp;task_id,&nbsp;task_tag, and&nbsp;description. Optional fields, in approximate order of usefulness:
Three LLM calls per task, then two per (table, pass).&nbsp;The table-selector runs once. Each&nbsp;(table, pass)&nbsp;runs a wide-net regex generation call and a (batched) relevance-filter call. All three use vLLM's strict&nbsp;json_schema&nbsp;response format so outputs are well-typed and don't require defensive parsing. Two passes per table.&nbsp;Standard&nbsp;*_concept_id&nbsp;columns hold the canonical OMOP mapping; source&nbsp;*_source_concept_idcolumns hold the original institutional / vocabulary codes (Epic EDG, ICD9CM source, MSDW, etc.). The source pass turns off&nbsp;standard_only&nbsp;and the domain filter, but restricts to vocabularies actually present on the source column at this site, then leans on the cohort match to prune. This catches non-standard codes that have no clean standard mapping (or that map up to a coarser parent). Descendant expansion via&nbsp;concept_ancestor.&nbsp;After the regex hits the concept table we expand each seed to its descendants before the cohort match. This is what catches brand-named drug products ("Baqsimi" for glucagon), SNOMED subtypes, and other hierarchy-only members that the concept-name regex would never see. The same filters used to produce the seed set are re-applied to descendants so they don't drift across categories. Each expanded concept gets a&nbsp;match_type&nbsp;column (regex_seed&nbsp;vs&nbsp;descendant) so QC can attribute final hits to either path. Wide-net LLM gets the vocab distribution.&nbsp;For each pass the LLM is handed the list of vocabularies on the column being searched with their concept counts and row counts. This lets it lean into the right vocabularies (RxNorm-shaped patterns for drug standard, ICD-shaped patterns for condition source) instead of guessing. The source pass also gets an explicit note that the candidates are non-standard and may use institution-specific phrasing. Cohort match drops dead candidates.&nbsp;If a concept has zero rows in the cohort we don't waste an LLM token asking whether it's relevant. Each surviving candidate carries a per-cohort row count so the relevance filter knows what's high-volume. Relevance filter is batched&nbsp;at 200 concepts per call. Each decision carries a one-line reason that gets persisted alongside the final list. Caching.&nbsp;The cohort, concept, and concept_ancestor tables are cached as parquet under&nbsp;sammy/prescreen/cache/&nbsp;so each task starts in seconds rather than re-reading the full vocabulary tables from&nbsp;output/. String-vs-int concept_id quirk.&nbsp;The vocab table stores&nbsp;concept_id&nbsp;as strings at this site.&nbsp;concept_table.load_concept&nbsp;and&nbsp;expand.load_concept_ancestor&nbsp;coerce to nullable&nbsp;Int64&nbsp;once at load time and cache the result; every downstream caller sees ints. Temporal anchoring (turn concept_id list into per-person event timestamps relative to a cohort index date). Out of scope for the prescreen — that's the next task.
Chunking the cohort-match parquet filter when the candidate set is large (&gt;~10k concept_ids). Rare but possible for very broad tasks or generous source passes.
A "second-look" loop on the wide-net regex: feed the LLM a small sample of&nbsp;concept_name&nbsp;rows that were&nbsp;near misses&nbsp;(matched include patterns but were knocked out by exclude patterns) and let it refine. Cheap and would catch poorly-worded exclude patterns.
Per-task cost telemetry: total LLM calls, total tokens, wall clock, written to&nbsp;run_summary.json&nbsp;for budget tracking across 100 tasks. ]]></description><link>index.html</link><guid isPermaLink="false">index.md</guid><pubDate>Wed, 13 May 2026 14:50:29 GMT</pubDate></item><item><title><![CDATA[README]]></title><description><![CDATA[Task-agnostic pre-screening pipeline for OMOP concept_id extraction. Runs once per task. Produces an exhaustive list of relevant concept_ids per OMOP table and per concept-id column, ready to be handed to downstream event-extraction code.For each clinical event-detection task (defined in a small YAML file):
Asks Nemotron which OMOP tables to search for this task.
For each selected table, runs&nbsp;two passes&nbsp;— one against the standard concept-id column, one against the source concept-id column.
Per pass: asks Nemotron for a wide net of regex patterns, applies them to the OMOP&nbsp;concept&nbsp;table, expands hits to their descendants via&nbsp;concept_ancestor, matches against the cohort's data, and asks Nemotron to filter the survivors for clinical relevance.
The output is a per-task directory with the final concept_id list, plus the intermediate artifacts at every stage of the funnel so you can audit and diagnose. ┌─────────────────────────────────┐ │ Nemotron #0: table selector │ one call per task └─────────────────────────────────┘ │ ▼ chosen tables │ ─── per (task, table) ───────────────────────────────────────────────────── │ splits into two passes: ┌─────────────────┬─────────────────┐ │ standard pass │ source pass │ │ *_concept_id │ *_source_concept_id │ domain filter on │ domain filter off │ standard_only=T │ standard_only=F │ └─────────────────┴─────────────────┘ │ ▼ vocab metadata for this column (which vocabularies live here) │ ▼ ┌─────────────────────────────────┐ │ Nemotron #1: wide-net regex │ └─────────────────────────────────┘ │ ▼ include_patterns / exclude_patterns ┌─────────────────────────────────┐ │ concept-table query │ filters: domain, └─────────────────────────────────┘ vocabularies, standard │ ▼ wide_net_seeds ┌─────────────────────────────────┐ │ descendant expansion │ via concept_ancestor; └─────────────────────────────────┘ re-applies filters │ ▼ wide_net_concepts (+ match_type: regex_seed | descendant) ┌─────────────────────────────────┐ │ cohort match │ count rows per concept_id └─────────────────────────────────┘ in cohort data │ ▼ cohort_match_concepts ┌─────────────────────────────────┐ │ Nemotron #2: relevance filter │ batched, json_schema strict └─────────────────────────────────┘ │ ▼ final_concepts (per pass) │ unioned across all (table, pass) into final_candidates.parquet
Each table has two searchable columns: the canonical standard&nbsp;*_concept_id&nbsp;and the source&nbsp;*_source_concept_id&nbsp;holding non- standard codes (Epic EDG, ICD9CM source, MSDW Attrib Catalog, etc.). Both are searched per task by default.The table-selector LLM call picks which of these to actually run for a given task. Override via&nbsp;tables:&nbsp;in the task YAML for explicit control.prescreen/ __init__.py config.py # paths + EVENT_TABLES (standard + source concept_col per table) cohort.py # load_cohort, cohort_ids (cached parquet) concept_table.py # cached concept table; coerces string concept_id -&gt; Int64 metadata.py # reads outputs/vocab_prescreen/{table}_*_vocab_type.csv vocab.py # query_concept(include, exclude, domain, vocabularies, standard_only) expand.py # descendant expansion via concept_ancestor cohort_match.py # count_concepts_in_cohort(table, candidate_ids, concept_col) nemotron.py # NemotronClient (mirrors clinical_extractor.client) llm_steps.py # prompts + json_schemas (table-selector, wide-net, relevance) pipeline.py # prescreen_one_table, prescreen_task, save_results tasks/ n_acut_06_hypoglycemia.yaml run_prescreen.py # CLI runner
diagnose.py # walk artifacts and find where a table's funnel collapsed
verify_coverage.py # check whether a known list of concept_ids was captured
# 1. Confirm the vLLM is reachable
python run_prescreen.py --healthcheck # 2. Run one task
python run_prescreen.py tasks/n_acut_06_hypoglycemia.yaml # 3. Fan out across all tasks
python run_prescreen.py tasks/*.yaml
Per task, under&nbsp;sammy/prescreen/tasks_out/{task_tag}/:hypoglycemia/ task.json run_summary.json final_candidates.parquet # unified across all (table, pass) # per (table, pass) artifacts — one set per pass: condition_occurrence__standard__patterns.json condition_occurrence__standard__vocab_metadata.parquet condition_occurrence__standard__wide_net_seeds.parquet condition_occurrence__standard__wide_net_concepts.parquet condition_occurrence__standard__cohort_match_concepts.parquet condition_occurrence__standard__decisions.parquet condition_occurrence__standard__final_concepts.parquet condition_occurrence__source__patterns.json condition_occurrence__source__vocab_metadata.parquet ... drug_exposure__standard__... drug_exposure__source__... measurement__standard__... measurement__source__...
final_candidates.parquet&nbsp;carries&nbsp;source_table,&nbsp;search_pass&nbsp;(standard&nbsp;/&nbsp;source), and&nbsp;source_concept_col&nbsp;columns so each concept tells you which (table, pass) it came from.diagnose.py {task_tag} {table}&nbsp;— walks the artifacts for one table, prints sizes at each stage of both passes, and tells you the first empty stage. Use it whenever a table comes back lighter than expected.python diagnose.py hypoglycemia condition_occurrence # both passes
python diagnose.py hypoglycemia condition_occurrence --pass source
verify_coverage.py {task_tag} --csv known_ids.csv&nbsp;— given a known list of concept_ids (e.g. from a gold-standard phenotype, a prior chart-review set, or an ad-hoc cohort check), reports what fraction the prescreen captured, broken down by which pass found them, and prints the names of anything missed.python verify_coverage.py hypoglycemia --csv my_known_glycemia_ids.csv
python verify_coverage.py hypoglycemia --ids 4129519,4226798,4280953
A minimal task only needs&nbsp;task_id,&nbsp;task_tag, and&nbsp;description. Optional fields, in approximate order of usefulness:
Three LLM calls per task, then two per (table, pass).&nbsp;The table-selector runs once. Each&nbsp;(table, pass)&nbsp;runs a wide-net regex generation call and a (batched) relevance-filter call. All three use vLLM's strict&nbsp;json_schema&nbsp;response format so outputs are well-typed and don't require defensive parsing. Two passes per table.&nbsp;Standard&nbsp;*_concept_id&nbsp;columns hold the canonical OMOP mapping; source&nbsp;*_source_concept_idcolumns hold the original institutional / vocabulary codes (Epic EDG, ICD9CM source, MSDW, etc.). The source pass turns off&nbsp;standard_only&nbsp;and the domain filter, but restricts to vocabularies actually present on the source column at this site, then leans on the cohort match to prune. This catches non-standard codes that have no clean standard mapping (or that map up to a coarser parent). Descendant expansion via&nbsp;concept_ancestor.&nbsp;After the regex hits the concept table we expand each seed to its descendants before the cohort match. This is what catches brand-named drug products ("Baqsimi" for glucagon), SNOMED subtypes, and other hierarchy-only members that the concept-name regex would never see. The same filters used to produce the seed set are re-applied to descendants so they don't drift across categories. Each expanded concept gets a&nbsp;match_type&nbsp;column (regex_seed&nbsp;vs&nbsp;descendant) so QC can attribute final hits to either path. Wide-net LLM gets the vocab distribution.&nbsp;For each pass the LLM is handed the list of vocabularies on the column being searched with their concept counts and row counts. This lets it lean into the right vocabularies (RxNorm-shaped patterns for drug standard, ICD-shaped patterns for condition source) instead of guessing. The source pass also gets an explicit note that the candidates are non-standard and may use institution-specific phrasing. Cohort match drops dead candidates.&nbsp;If a concept has zero rows in the cohort we don't waste an LLM token asking whether it's relevant. Each surviving candidate carries a per-cohort row count so the relevance filter knows what's high-volume. Relevance filter is batched&nbsp;at 200 concepts per call. Each decision carries a one-line reason that gets persisted alongside the final list. Caching.&nbsp;The cohort, concept, and concept_ancestor tables are cached as parquet under&nbsp;sammy/prescreen/cache/&nbsp;so each task starts in seconds rather than re-reading the full vocabulary tables from&nbsp;output/. String-vs-int concept_id quirk.&nbsp;The vocab table stores&nbsp;concept_id&nbsp;as strings at this site.&nbsp;concept_table.load_concept&nbsp;and&nbsp;expand.load_concept_ancestor&nbsp;coerce to nullable&nbsp;Int64&nbsp;once at load time and cache the result; every downstream caller sees ints. Temporal anchoring (turn concept_id list into per-person event timestamps relative to a cohort index date). Out of scope for the prescreen — that's the next task.
Chunking the cohort-match parquet filter when the candidate set is large (&gt;~10k concept_ids). Rare but possible for very broad tasks or generous source passes.
A "second-look" loop on the wide-net regex: feed the LLM a small sample of&nbsp;concept_name&nbsp;rows that were&nbsp;near misses&nbsp;(matched include patterns but were knocked out by exclude patterns) and let it refine. Cheap and would catch poorly-worded exclude patterns.
Per-task cost telemetry: total LLM calls, total tokens, wall clock, written to&nbsp;run_summary.json&nbsp;for budget tracking across 100 tasks. ]]></description><link>readme.html</link><guid isPermaLink="false">README.md</guid><pubDate>Wed, 13 May 2026 14:50:29 GMT</pubDate></item></channel></rss>